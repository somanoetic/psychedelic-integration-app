# Project Log

## 2026-06-18 — Added double-tap-to-hide on the floating Huxley button

Added a quick way to tuck away the floating Huxley button that sits over every screen: double-tapping it now slides it off to the edge, the same as the existing long-press and drag-to-edge gestures. A single tap still opens the little menu as before, with no added delay. This hasn't been tried on a phone yet — just confirmed it compiles.

## 2026-06-18 — Fleshed out the "Tools & Daily Practices" learning articles and made them interactive

The four "Tools & daily practices" articles (habits, cognitive distortions, contemplative practices, and ACT) were reading as thin fly-overs, so we rebuilt all four to the same depth and book-sourced quality as the recent Body/Brain set — about double the length each, with named authors, a hands-on "Try this," and links to matching exercises. The cognitive-distortions article got the most love: a real interactive layer. There's now a tappable flashcard deck of thinking traps and a "name the distortion" quiz, and — following your lead on the Harvey Norris flashcard book — we added two more decks (cognitive biases and logical fallacies) plus a second quiz, all reframed for inner work and sourced from material already in your library. Along the way we fixed two display bugs you spotted: bold/italic text was showing raw asterisks (the text styler only understood bold, now it does italics too), and tapping a "see also" link dropped you at the bottom of the new article instead of the top. Everything is checked to compile, but none of it has had a real walk-through on a phone yet — that's the main thing still owed. The Harvey Norris book itself couldn't be used directly (you only have the physical copy), so it's parked as something to scan in later if we want to extend the decks.

## 2026-06-18 — Added practices to the learning articles and shipped them to the main branch

Finished the "Body, Brain & Healing" rebuild by giving every one of the 8 articles a short hands-on "Try this" practice plus tappable links to matching exercises from the app's existing library — so a reader can go straight from learning into doing. Then merged the whole batch into the main line and pushed it. One thing to flag: the merge also carried along the earlier "Attachment Reflection" feature (it was sitting underneath this work in the branch history), so that's now on the main branch too even though it still hasn't had a real walk-through on a phone. Bottom line on what's been checked: only the first article has been seen on a device — the other seven articles and the Attachment Reflection feature still want a device pass.

## 2026-06-18 — Rebuilt the "Body, Brain & Healing" learning section into 8 book-sourced articles

Grew the "Body, brain & healing" part of the Learn hub from 4 articles to 8, with every claim checked against your imported library of books rather than written from memory. The four originals (Somatic, Your Brain on Healing, Trauma, Attachment) were rebuilt, and four new ones were added: Your Nervous System & Safety, The Mind-Body Connection & Chronic Pain, How Emotional Learnings Change, and Mind, Brain & Relationships. For each article a dedicated reader went through the actual source texts and flagged anything the books didn't truly support, so we corrected real overclaims the old articles had been making — softening "repetition rewires your brain," handing the memory-reconsolidation explanation to the article that's actually about it, and writing the brain's "sense of self" section carefully from the two philosophy books we have rather than asserting a brain mechanism we can't yet cite. The pain article, the riskiest one, now leads with "see a doctor first" and never suggests anyone stop treatment. Every article also got a short hands-on "Try this" practice, tappable links to matching exercises from the app's existing library, "see also" links between related articles, and a plain "drawn from" sources line. Two gaps are parked in a backlog doc: the default-mode-network section still needs a psychedelic-neuroscience book before it can be written properly, and the article ordering can be regrouped later if you prefer. Committed to its own branch; not yet checked on a device beyond the first article.

## 2026-06-18 — Built a guided "Attachment Reflection" conversation with Huxley

Turned the clinician's Adult Attachment Interview into a warm, guided Huxley session that anyone can do on their own. Instead of diagnosing anyone, it walks you through the same arc of questions — who raised you, five words for each parent and a memory behind each, key childhood moments, and how it all shaped you — as a reflection, never a label. On the back end, Huxley quietly notes a tentative attachment pattern for the practitioner's eyes only; it's saved with the session but never shown to the user, and we added a test that proves it can't leak into the conversation. It's wired into the Inner Work hub, the database table is set up, the automated tests pass, and it's committed to its own branch — but it still needs a real walk-through on a device, for which there's a step-by-step test guide.

## 2026-06-18 — Rebuilt the app's research library search and improved its citations

The app already has a "knowledge base" — your imported library of ~280 books and papers, indexed so the AI guide can pull relevant passages. We discovered it was already built and working, so instead of building a new one, we fixed two quality problems and made it searchable from our chats. First, citations were broken: a quoted passage would say "pages 2 through 266" (basically the whole book) instead of the actual page, so we fixed it to cite the real page — now most passages point to a single page. Second, 40% of the material had been dumped into a generic "miscellaneous" bucket, which made topic-filtered search miss things; we re-tagged everything by subject, dropping that to about 7% and bringing whole topics (Jungian work, mind-body pain, attachment) back into reach. We also chased down what looked like garbled text and confirmed it was a false alarm — the data was clean all along, just a quirk of how one terminal displays special characters. The reindex is done, search is verified, and a small command-line tool now lets us search the library together while drafting articles.

## 2026-06-18 — Cleaned up the legal pages and renamed the app to Multitudes everywhere

Two connected pieces of work. First, the privacy policy and terms of service: since paying for a full lawyer isn't in the cards right now, we did the corrections we could do ourselves and wrote up a clear packet so a cheap templated service (and, later, a focused budget legal review) can finish the job. We fixed real errors — the policies didn't mention two outside services that actually receive data (OpenAI and the crash-reporter Sentry), claimed export worked one way when the app does it another, and contained a made-up legal reference — and we added a section for Washington's strict health-data law, named Massachusetts as the governing state, and noted that the trickier items should get a paid set of eyes before the app starts charging money. Second, we finished renaming the product from "Huxley" to "Multitudes" in everything users see, while keeping "Huxley" as the name of the in-app AI guide itself — so the splash screen, home header, disclosure screen, and contributor pages all say Multitudes now. Everything was committed to a new branch (not pushed yet), and we deliberately left your other in-progress work alone.

## 2026-06-18 — Planned a book-sourced rebuild of the "Body, Brain & Healing" learning articles

Worked through how to make the "Body, brain & healing" section of the Learning Hub more organized and thorough, using your imported library of books as the source of truth. We decided to grow it from 4 articles to 8 — rebuilding the four existing ones (Somatic, Brain on Healing, Trauma, Attachment) and adding four new ones (Nervous System & Safety, Mind-Body & Pain, How Emotional Learnings Change, and Mind/Brain/Relationships). Along the way we double-checked two of your hunches against the actual books and you were right both times: the attachment article keeps the standard four styles (they're covered in Siegel's book), and the brain article will present both the older "emotion circuits" view and the newer "emotions are constructed" view rather than picking one. No app code was changed yet — everything is captured as detailed, book-grounded outlines for you to review and edit before we start writing. A couple of side ideas were parked: turning the clinician-side Adult Attachment Interview into a guided Huxley session, and building a proper index of your source library in a future chat.

## 2026-06-17 — Fine-tuned the home screen icon sizes

Small visual polish on the home screen. The Track icon at the top was smaller than the rest, so we bumped it up to match the Prepare tile. We also nudged a couple of the tile icons down a touch — Process & Integrate by 10% and Inner Atlas by 20% — to balance the grid. You confirmed it looks better. The changes were already saved and are on the remote.

## 2026-06-17 — Prettier log confirmations and fixed a cut-off learning page

Two fixes today. First, when you log a moment in any of the trackers (nervous system, glimmer, trigger, parts), the little "saved" pop-up now has a softer, on-brand look with a green check, and tapping its button takes you back to the home screen instead of just sitting on the form. Second, the Learning Hub sub-pages (Self-Discovery Tools, Body/Brain & Healing, and the rest) were cutting off the bottom tile on the device — added proper spacing at the bottom so the last tile clears the screen edge. The first batch was confirmed working on your device; the learning-page fix is in but worth a final glance.

## 2026-06-16 — Sharpened the AI library and reworked the home screen

Two things. First, the quick win from the audit: the research library's search index was under-sized for how much got loaded, so we rebuilt it on the real data and widened how much it scans per question — Huxley should now surface more of its best matches. That's live in the database and committed. Second, the home screen: there were five tiles, which left an awkward gap, so we restructured the whole thing. The bottom menu bar is now down to three (Home, Journal, Inner Atlas), and Learn and History moved up to become tiles. The grid now reads cleanly — two tiles, a full-width History band, then two more rows — with a new History icon. It's committed and pushed but hasn't been checked on a real device yet, so the tile taps (especially Learn and History) are worth a look.

## 2026-06-16 — Audited the backlog; found the knowledge base is actually live

You suspected a lot of the open to-do items were already finished, and you were right. We went through everything the tracker still listed as "open" and checked it against the real code. Three items were already done and just never closed out, and several others were finished in the code with only account or app-store steps left for you. The big one: the AI knowledge base (the research library Huxley draws on) was marked as "not deployed yet," but it's fully live — over 21,000 passages from 281 books and documents are loaded and searchable, and a test question pulled back exactly the right material. The one real caveat is that its search index looks under-sized for how much got loaded, so it may be missing some of its best matches; a quick 30-second fix in the database dashboard would sharpen it. We updated the tracker to reflect all of this.

## 2026-06-16 — Merged the trackers into one "Track" block on the home screen

Reworked the top of the home screen. The three status widgets (nervous system, habits, glimmers) and the separate "Track" tile were doing overlapping jobs, so we combined them into a single Track block: a header with the Track icon and title, plus a row of five small status readouts underneath — one for each tracker (nervous system, glimmer, trigger, parts, habits) — that shows when you last logged each. Tapping anywhere on the block opens the same Track menu as before, and the main tile grid is now five consistent sections. Then we spent a while tuning the look: stacked the header, sized the icons so the faint glimmer and trigger ones match the others, lightened the box, and stripped the border/shadow that the side icons were bumping into. It's built but not yet checked on a real device.

## 2026-06-16 — Restyled the session checklist to match the rest of the app

The session checklist screen had two heavy yellow-to-blue gradient blocks — the top bar and the progress card — that clashed with the calmer, softer look the rest of the app moved to. Restyled both to match the Sessions hub: a soft full-screen background, a clean dark title, a white progress card, and category headers that now read as distinct white rows. It's a visual-only change and hasn't been run on a device yet, so it's worth a quick look before committing.

## 2026-06-16 — Verified caching, fixed the Set Your Intention chat input

Confirmed on a real device that the money-saving prompt caching is genuinely working — the dev logs showed Huxley reusing its long instructions on every follow-up message instead of being re-billed. Then fixed the one chat that still looked wrong: on the Set Your Intention screen, the text box sat partly behind the bottom navigation bar and used the old keyboard handling the other chats had already moved off of. It now sits above the nav bar and clears the keyboard cleanly, matching every other chat. Also reverted an accidentally-swapped splash icon. The chat fix is committed; the earlier keyboard and caching work turned out to have already been committed.

## 2026-06-06 — Finished the chat keyboard fix + confirmed caching saves money

Closed out two things that were waiting on a real-device check. First, the keyboard fix: the main Huxley chat screen had its own copy of the old keyboard handling, so we ported the same fix the conversation screens already had. On the device, the text box was getting partly hidden behind the keyboard, so we corrected how much it lifts — it now sits fully above the keyboard on both the main chat and the conversation screens, and there's no leftover gap when the keyboard closes. Second, while testing we watched the dev logs confirm the prompt caching from earlier is genuinely working — Huxley's long instructions are being reused from cache on every follow-up message instead of being re-billed. Both fixes are tested on a real Android device but not yet committed.

We did the work the earlier audit pointed to: turning on prompt caching so Huxley's long, repeated instructions aren't re-billed on every message. The trick was to split each prompt into a stable part (cached and reused) and a per-message part (the fresh research snippets and safety checks), and move the changing part into the user's message instead of the instructions. This now covers both Huxley's main chat and the routing/triage chat, and the proxy and server function were wired up to support it and to log how often the cache is hitting. Tests pass; the one piece still outstanding is confirming on a real device that the cache is actually saving money (you'll see it in the dev logs after two quick back-to-back messages).

## 2026-06-05 — Fixed the chat keyboard gap

Fixed the long-standing bug where, on the conversation screens, a blank space was left below the text box after you closed the keyboard (and the box sometimes floated or got covered). The root cause was the keyboard-handling code fighting with Android's own screen-resizing, leaving leftover padding. We rewrote it so the text box now sits correctly at rest, clears the keyboard when typing, and snaps back with no gap when the keyboard closes. The change is in one shared component, so it covers all the conversation screens at once — but it still needs a real-device check, and the main Huxley chat screen uses its own separate keyboard code that may need the same fix.

## 2026-06-05 — Checked which AI prompts can be cached

We looked at whether Huxley's behind-the-scenes instructions stay the same enough between messages to safely turn on prompt caching (a cost-saver). The short answer: not yet, for the two parts of the app that matter. Huxley's main chat and the routing/triage chat both mix in stuff that changes with every message — the freshly-pulled research snippets and the message-specific safety checks — right in the middle of the instructions, which defeats caching. To make caching pay off, that changing material needs to move into the user's message instead. No code was changed; this was an audit only, with a clear list of what to fix.
